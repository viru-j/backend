server:
  port: 8081
logging:
  level:
    root: INFO
neo4j:
  uri: ${NEO4J_URI:bolt://localhost:7687}
  user: ${NEO4J_USER:neo4j}
  password: ${NEO4J_PASSWORD:password}
lucene:
  index-path: ${INDEX_PATH:/data/mcp/index}
